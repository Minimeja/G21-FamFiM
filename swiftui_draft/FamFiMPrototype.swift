import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("FamFiM Prototype")
                .font(.largeTitle)
                .padding()

            Text("Dashboard Screen")
                .font(.title2)
        }
    }
}